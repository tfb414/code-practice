import re

def find_uniq(arr):
    def checkIfUnique(arr, n):
        unique = True
        for i in range(len(arr)):
            if arr[i+1] == n
            unique == false
        return unique
    
    arrList = list(arr)
    answer = filter()
    
    print derp

find_uniq([ 1, 1, 1, 2, 1, 1 ])


incomplete