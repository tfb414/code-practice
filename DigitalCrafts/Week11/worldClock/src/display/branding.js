import React, { Component } from 'react';
import logo from '../logo.svg';

const Branding = () => (
    <div className="App-header">
        <img src={logo} className="App-logo" alt="logo not here" />
        <h2>Time to figure out the time</h2>
    </div>
);

export default Branding;